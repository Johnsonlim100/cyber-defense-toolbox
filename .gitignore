__pycache__/
*.pyc
.ipynb_checkpoints/
venv/
.venv/
.env
.DS_Store

# NOTE: model files (*.joblib, *.pt, *.csv datasets) are intentionally
# NOT ignored — they must be committed for the cloud deploy to work.
